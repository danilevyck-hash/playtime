'use client';

// Instagram Reels section — disabled because thumbnails consistently fail to load.
// Kept as a placeholder; returns null so the section is hidden.
export default function InstagramFeed() {
  return null;
}
